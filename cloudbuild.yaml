steps:
  - name: node:14.17.6
    entrypoint: sh
    args:
      - -c
      - |
        npm install
        npm run build

  - name: bash
    script: |
        npm install -g firebase-tools
        firebase deploy --token=$FIREBASE_TOKEN --only hosting
